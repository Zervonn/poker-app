<header class="global-header">
    <div class="logo">🃏 Planning Poker</div>
    <div class="header-right">
      <% if (typeof user !== 'undefined') { %>
        <span id="user-label">Logged in as: <%= user %></span>
      <% } %>
      <button id="theme-toggle" class="theme-toggle">🌓</button>
    </div>
  </header>
  